# Kubernetes Namespace for FinTech Application
# Namespaces provide logical separation and resource isolation

apiVersion: v1
kind: Namespace
metadata:
  name: fintech
  labels:
    name: fintech
    environment: production
    team: platform
